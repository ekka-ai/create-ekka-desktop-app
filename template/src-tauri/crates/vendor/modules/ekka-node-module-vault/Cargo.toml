[package]
name = "ekka-node-module-vault"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "EKKA Node Vault Module - Safe, read-only vault status with no path exposure"

[dependencies]
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Async runtime
tokio = { version = "1.0", features = ["full"] }

# Web framework
axum = "0.7"

# Logging
tracing = "0.1"

# Module interface
ekka-node-modules = { path = "../../framework/ekka-node-modules" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
